% Constraint Logic Programming over rational and real numbers.
% Both libraries define the predicate {}/1 and thus you can select
% only one.
:- use_module(library(clpq)).	% Constraints over Q (rational numbers)
%:- use_module(library(clpr)).  % Constraints over R (real number)

% Your program goes here


/** <examples> Your example queries go here, e.g.
?- { X = 2 * Y, Y = 1 - X }.
*/
